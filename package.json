{
  "name": "client",
  "proxy": "http://localhost:4000/",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && npm start"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
